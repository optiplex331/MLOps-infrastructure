#!/usr/bin/env python3
"""Historical executable for the former CPU-only release-lab seam."""

from pathlib import Path
import sys

REPOSITORY_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPOSITORY_ROOT / "src"))

from mlops_infrastructure.synthetic_release import main  # noqa: E402

raise SystemExit(main())
