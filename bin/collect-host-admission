#!/usr/bin/env python3
"""Collect sanitized admission evidence on the declared native-Ubuntu host."""

from pathlib import Path
import sys

REPOSITORY_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPOSITORY_ROOT / "src"))

from mlops_infrastructure.host_admission import main  # noqa: E402

raise SystemExit(main())
