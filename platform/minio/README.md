# Historical MinIO Surface

This directory is retained for provenance from the former CPU release-lab
workflow. It is not an active Phase 1 deployment entrypoint and is not
rendered by the default CI workflow.
